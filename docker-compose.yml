version: '3.8'

services:
  textpod:
    image: namvu87/textpod-daisyui:latest
    build: .
    ports:
      - '3000:3000'
    volumes:
      - './data:/app'
    container_name: textpod-daisyui
    restart: unless-stopped