services:
  app:
    image: ghcr.io/vnt87/textpod-toc:latest
    ports:
      - "8818:3000"
    volumes:
      - textpod_data:/app/data
      - ./backups:/app/backups
    environment:
      - LISTEN=0.0.0.0

volumes:
  textpod_data:
    name: textpod_data
